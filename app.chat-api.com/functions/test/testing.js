"use strict";
//libs
//modules
const myFunctions = require('../index');
//init
describe('Reserve new instance',()=>{
    it('Should reserve instance if available');
    it('Should do nothing if no instance available');
    it('Should send email with count of available slots');
});